{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "nixpacksConfig": {
      "phpVersion": "8.4"
    },
    "buildCommand": "composer install --no-dev --optimize-autoloader --ignore-platform-reqs && npm ci && npm run build",
    "watchPatterns": [
      "**"
    ]
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan cache:clear && php artisan migrate --force && php -S 0.0.0.0:$PORT -t public",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  }
}
